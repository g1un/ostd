To run the page unzip ostd.zip and open index.html.
I wrote code in scss and compiled it into css (included).
There are no json packages in project block, since I have never dealt with json, except maps api (don’t know if it can be called json package). So I’ve done project block using random articles.
Transition from mobile to desktop version will occur when window width becomes 641px.
I’ve also placed my “homework” here – http://test.g1un.ru/
